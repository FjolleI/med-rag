fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<1
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
python-dotenv>=1.0
httpx>=0.27,<0.28

# MCP server (Model Context Protocol)
mcp>=1.9,<2

# Real backends (mock fallback if keys are placeholders)
pinecone>=6.0,<10
openai>=1.55,<2

# Persistence (kept for future Postgres/Redis wiring; runtime-optional)
sqlalchemy>=2.0,<3
psycopg2-binary>=2.9
redis>=5.0,<6

# Auth helpers (kept for the planned JWT layer)
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# Tests
pytest>=7.4
pytest-asyncio>=0.21
